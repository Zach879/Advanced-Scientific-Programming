This project extends the provided data‐generation script to add normal, exponential and log‐scaled distributions, hooks in ARFF/CSV 
 conversion via the existing library, and automates statistical summaries and histogram plots—by modifying only the main assignment file.
